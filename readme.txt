Class Matrix

1. Increment and decrement (both forms), which change all elements by 1
2. Comparison operators for two matrices (== and =!)
3. Logical operators (<, <= , > , >=)
4. Matrix c = Matrix b 
5. The operator [], which returns a string in the form of a one - dimensional array"
6. Operator() to access any matrix element
7. Type to int conversion operator (sum of all matrix elements)
